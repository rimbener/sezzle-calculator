# review-slice-3 — busy state, error presentations and docs

- **task:** p4-calculator-api-client
- **slice:** 3 — busy state, error presentations and docs (subtask-3, AC-11, AC-12, AC-13, AC-14, AC-15)
- **verdict:** CHANGES_REQUESTED
- **suite:** `npx turbo run test --output-logs=errors-only` — 5/5 tasks successful, all cached (`FULL TURBO`); green. The npm `EBADDEVENGINES` warning (npm 12.0.2 vs `^10.0.0`, `onFail: warn`) is the documented working-as-intended warning, not a finding.

## Trail

- Diff base: slice 2's `closing-commit: fc845cb`, read from `tdd-2.md:22` — trail intact. Diff = `fc845cb`..worktree plus untracked `tdd-3.md`. Commit `76c04b5` is the trail bookkeeping recording slice 2's record and resolved review — no findings raised on it.
- Slice 3's diff: `display.ts` (the one reserved line, `pending` → `state: 'busy'`), `display.test.ts` (pending it.each flipped to `'busy'`), `Calculator.test.tsx` (`displayState()` taught the busy class; the AC-25 block gains `expect(displayState()).toBe('busy')`, the AC-18 block flips `idle` → `busy`), `App.test.tsx` (the AC-12 four-presentation it.each, the AC-13 recovery describe), `README.md`, `scope-docs.test.ts` (the AC-14 README pins and the AC-15 AGENTS.md describe), `AGENTS.md`'s opening sentence, plus `subtask-3.md`'s status flip and `tdd-3.md`.
- `refactor:` — subtask-3 carries none ("nothing to pin"); nothing to check, nothing omitted. No `preserves:` clause exists to rule on, and no behavior change accompanied the pending→busy projection that any red test could be hiding — the suite is green.

## Findings

- **F1 — [lens 1, test-step] [major] [resolved] — AC-15's `build` leg is exercised by no recorded command in this slice.** `acceptance-criteria.md:27` requires root `lint`, `check-types`, `test` **and `build`** all passing. `tdd-3.md:9` maps AC-15 to `npx turbo run lint check-types test --output-logs=errors-only` — 15/15 tasks — and `tdd-3.md:18` states the build leg "is not in this invocation's command list; it rides the fix step's gate". No fix step has run yet for slice 3 (this is its review), so if this review approved clean the build clause would rest on nothing exercised — the same gap slice 2's F1 (`review-slice-2.md:16`) blocked on, where the fix recorded the build leg in the map. This review's Commands covered `test` only, and `turbo.json` (unchanged since slice 2's note) gives `test` no `dependsOn` that would pull `build` in. The leg rests on the record's claim alone, which is exactly what lens 1 refuses to pass on. Fix: run the gate with `build` included and record the build leg in AC-15's map, as slice 2's F1 did for AC-10. (The projection change is one type-compatible line — production code is not implicated; the fix is the gate, hence test-step.)

No other findings:

- **Lens 1 (criterion → test map):** every AC maps to a concrete test present in the diff. AC-11 → `display.test.ts:28-41` (pending holds the calculation, state `'busy'`, it.each over add/sqrt/percentage), `Calculator.test.tsx:140` (AC-25 through the UI) and `Calculator.test.tsx:164-177` (AC-18: `7`, `.`, `Add`, a second `Square root`, a second `Equals`, `Clear` all land while pending and `onRequest` stays at exactly 1 — the second-`=`/second-unary half of AC-11 is genuinely exercised through the UI, with `reducer.test.ts:186` still covering the unit level). AC-12 → `App.test.tsx:72-128`, one end-to-end case per row of `spec.md`'s error-contract table: 422 `DIVISION_BY_ZERO` relayed as `cannot divide by zero`, 502 outage → `OUTAGE_MESSAGE`, thrown `fetch` → `NETWORK_MESSAGE`, stray 400 → `UNEXPECTED_MESSAGE`; each asserts the exact message in the value slot over the failed calculation, in the error state — no blank screen, no raw exception text. AC-13 → `App.test.tsx:130-246`: a digit after each of the four errors, the decimal point, `C` after an error, `C` after a result. AC-14 → `scope-docs.test.ts:107-129`, four pins, all satisfied by the README diff. AC-15 → `scope-docs.test.ts:67-79` plus the gate (build leg = F1).
- **Lens 2 (project conventions):** the diff stays inside subtask-3's seven paths; the reducer, key model and every other projection case are untouched (spec non-goal respected — the one `pending`/`busy` line is all `display.ts` changes); new test code follows the app's flat style; the AC-25 busy assertion's addition (where the subtask's prose expected a flip) is documented in `tdd-3.md:13`, serves AC-11 through the UI, and adds nothing the spec doesn't call for.
- **Lens 3 (user surface):** the slice touches the SPA's display — its output matches the approved spec exactly. The three rewritten messages are the client's own constants (`client.ts:40,43,46`) and match the spec's quotes character for character; the domain message is relayed unchanged; the busy indicator is real, not just a class assertion — `packages/ui/src/display.tsx:19` swaps the meta line `Ready` → `Working` when state is `busy`, which is what the README now promises. No new names beyond slice 1's message constants.
- **Lens 4 (docs parity):** the behavior change (busy state) and both docs updates are in this same diff. Every checked README claim is true against the code: the client/hook description, `VITE_GATEWAY_URL` defaulting to `http://localhost:3000`, "the client never rejects", "no component calls `fetch`" (the pre-existing `frontend-purity.test.ts` scan enforces it and is green); `AGENTS.md:7` now says the SPA calls the gateway through `src/api/client.ts` and `useCalculate`, its only network call the one `POST /api/v1/calculate` — and `scope-docs.test.ts:67-79` pins the correction.
