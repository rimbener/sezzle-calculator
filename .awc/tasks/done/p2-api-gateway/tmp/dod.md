# dod — p2-api-gateway

**Verdict: PASS** — every dimension holds with re-run evidence. No `test-step` item, no production-work item. One merge-time note for the human PR step (§ Merge note), not a DoD failure.

Invocation: `Task: p2-api-gateway. Mode: validate. Commands: npx turbo run lint check-types test --output-logs=errors-only. Base: main.` Validation only — no code, test, branch, commit or PR touched; working tree clean before and after (`git status --short` empty).

## Checklist

| | Dimension | Evidence |
| --- | --- | --- |
| [x] | **Functionality** | All 21 criteria trace to passing tests (table below); the one `refactor:` entry (subtask-1, `STATUS_BY_CODE` moved into the contract) landed — `apps/calc-service/src/app.ts` imports it from `@repo/contracts` and drops the local map + `hono/utils/http-status` import (merge-base diff) — with its `preserves:` clause pinned by calc-service's unchanged `app.test.ts` (14 files / 90 tests green) and the live smoke: `divide [7,0]` → `422 DIVISION_BY_ZERO "cannot divide by zero"` through the gateway. Live smoke under Node 22.23.2 hit every row of `spec.md` § Error contract: 200 for `percentage [15,200]` → `{"result":30}`, `add` → 5, `sqrt [16]` → 4, `divide [1,3]` → `0.3333333333333333` (relayed, not recomputed); 400 ×3 with the contract's exact sentences and `request body must be valid JSON`; 422 ×3 (`DIVISION_BY_ZERO`, `NEGATIVE_SQRT`, `RESULT_NOT_FINITE`); calc-service stopped → `502 "calculation service is unreachable"` in 0.116 s (one retry) while a 400 still answers; hanging downstream with `CALC_SERVICE_TIMEOUT_MS=300` → `504 "calculation service did not respond in time"` in 0.32 s / 0.31 s; recovery (AC-21) 502, 502, then `200 {"result":42}` once calc-service came back, no gateway restart; `GET /nope` → Hono's default 404 (P1 envelope not built, per spec § Non-goals); SIGTERM released the port. |
| [x] | **Conventions** | `apps/api-gateway/{tsconfig.json,vitest.config.ts,eslint.config.mjs}` are byte-identical to calc-service's (`diff` → IDENTICAL); `package.json` differs only in `name`; `prettier --check` over `apps/api-gateway/**/*.{ts,md}`, `packages/contracts/src/*.ts`, `apps/calc-service/src/*.ts` → "All matched files use Prettier code style"; `eslint . --max-warnings 0` exit 0 in all four touched workspaces (run summary). Build-free like calc-service: `dev`/`start` run `src/server.ts` directly, `.ts` extensions on relative imports (`app.ts:16`, `server.ts:3-5`). |
| [x] | **Architecture & dependencies** | Neither service imports the other: `no-arithmetic.test.ts:41` restricts every gateway import to `@repo/contracts`, `hono*`, `@hono/node-server`, `node:*`, vitest or a sibling (passing); `grep` of gateway non-test sources finds no `Math.` and no arithmetic. Route is parse/call/serialise only (`app.ts:78-110`); failure logic in `calc-client.ts` (`classify` :60-74, deadline/retry :111-165). Only `config.ts:14,16` carry URLs, as env defaults (`CALC_SERVICE_URL`, `CORS_ORIGIN`) — no hardcoded cross-service URL. Contract depends on `zod` alone (`packages/contracts/package.json` absent from the diff). Dependency diff `main...HEAD`: `apps/api-gateway/package.json` (new; same deps and ranges as calc-service's), `package-lock.json` +38/−0 — the workspace block, nested `@types/node` 22.20.1 + `undici-types` 6.21.0 whose `resolved`/`integrity` equal calc-service's nested pair (lockfile :38-51 vs :72-85), and the `node_modules/api-gateway` link; the sorted set of `node_modules/*` names at merge-base vs HEAD differs by exactly that one link. No new third-party package, no version change, no override/`.npmrc`/`turbo.json`/root `package.json` change. Named with a verdict in `review.md` § Lens 2 and recorded in `spec.md` § Surfaces touched ("root `package-lock.json` — the new workspace and its dependencies") and § Approach ("No new dependency"). |
| [x] | **User surface** | `POST /api/v1/calculate` on `GATEWAY_PORT` (default 3000) with the four env vars of `spec.md` § Configuration — `config.ts:8-16` and `config.test.ts` (4 tests) match the table verbatim; CORS: preflight from `http://localhost:5173` answered 204 with `access-control-allow-origin: http://localhost:5173`, `allow-methods: POST,OPTIONS`, `allow-headers: content-type`; preflight from `http://evil.example` got no `allow-origin` header (smoke). Docs landed: `apps/api-gateway/README.md` (what it is / only public service :3-8, start :19, test :98, the four variables :30-35, curl for 200/400/422/502 :45-80, 504 sentence :83-87 — each quoted body matches what the smoke returned); `AGENTS.md` § What this is, Node bullet, suites list, `apps/api-gateway` layout entry, eslint-config consumers (merge-base diff, 13 lines); `packages/contracts/README.md` names the response schemas and `STATUS_BY_CODE`. |
| [x] | **Security** | No secret anywhere in the diff (`grep -iE 'api[_-]?key|secret|password|token'` hits only `app.test.ts`'s deliberate leak-probe string and `review.md` prose). No user-controlled value reaches a path, command or query: request data goes only to `JSON.parse` → Zod → a re-serialised body (`calc-client.ts:119-124`); the only spawn is the test's fixed argv (`server.test.ts:115`). `app.onError` (`app.ts:74-76`) maps any residual throw to the bare envelope; `app.test.ts:281` proves a rejecting client leaks no `fetch`/path/`ECONNRESET` text. Resources cleaned: deadline timer cleared in `finally` (`calc-client.ts:162-164`), abort reaches the socket (`:123`), `server.close` awaited (`server.ts:30-33`), test child killed in `afterEach` (`server.test.ts:107-110`); smoke confirmed SIGTERM releases the bound port. CORS exact-origin, no credentials. |
| [x] | **Testing rigor** | Gate re-run twice with cache defeated: `npx turbo run lint check-types test --output-logs=errors-only --force` under the shell's Node 24.20.0 → 15/15, 0 cached, exit 0; and `--force --summarize` under the pinned line Node 22.23.2 / npm 10.9.8 → 15/15, every task `cache MISS`, every `exitCode 0` (`.turbo/runs/3IvSHSrlLKOtSwGBfLqTYsKYzbs.json`; ignored path, tree still clean). Vitest per turbo's task logs: api-gateway 5 files / 86 tests (`app.test.ts` 45, `calc-client.test.ts` 28, `server.test.ts` 6, `config.test.ts` 4, `no-arithmetic.test.ts` 3); calc-service 14 / 90; contracts 5 / 100; `sezzle-calculator` and `@repo/ui` "No test files found" (unchanged from base). The first, plain run of the exact command was a `FULL TURBO` replay (15 cached, 6 ms) — recorded, not relied on. Criterion → test map in `tdd-1/2/3.md` re-checked against the files (table below). |
| [x] | **Observability & docs** | The one log the design names — the announce line — is `api-gateway listening on port <n>` (`server.ts:42`), pinned by `server.test.ts:126` and seen live (`api-gateway listening on port 43110`). No other logging (BE-12 is P1, per spec § Non-goals) and none was added. Docs consistent with code: README variable table ≡ `config.ts` defaults; `AGENTS.md:52` describes `createApp({ calcClient, corsOrigin })`, which is `app.ts:58`'s real signature; README's quoted 400/422/502 bodies are the smoke's actual bodies. |

## Criterion → passing test (re-verified in the files and in the green run)

| AC | Test(s) |
| --- | --- |
| AC-1 | `packages/contracts/src/errors.test.ts:19` ("error contract (AC-3)" — six codes), `:55` ("fixes the two SERVICE_UNAVAILABLE sentences (p2 AC-1)") |
| AC-2 | `calculate.test.ts:144` ("calculateResponseSchema — parsing a downstream reply (p2 AC-2)"), `errors.test.ts:72` ("errorResponseSchema — parsing a downstream envelope (p2 AC-2)") |
| AC-3 | `errors.test.ts:124` ("STATUS_BY_CODE — each code's default status (p2 AC-3)"); `apps/calc-service/src/status-map.test.ts:29` (repo scan: exactly `packages/contracts/src/errors.ts` declares it) |
| AC-4 | calc-service `app.test.ts` unchanged in the diff, 90/90 green; `README.md`, `turbo.json`, root `package.json`, `apps/sezzle-calculator`, `packages/ui` absent from `main...HEAD`; neither SPA nor `@repo/ui` `package.json` names the contract or a service (at merge-base). `npm run build` clause: not in `Commands:`, not run; `build` exists only in `apps/sezzle-calculator/package.json`, whose sources are untouched by this diff and whose `tsc -b` (`check-types`) passed in the gate |
| AC-5 | `apps/api-gateway/src/config.test.ts:15` ("resolveConfig (AC-5)", 4 tests) |
| AC-6 | `calc-client.test.ts:49` ("the request (AC-6)") |
| AC-7 | `calc-client.test.ts:77` ("a recognisable answer (AC-7)") |
| AC-8 | `calc-client.test.ts:114` ("every other answer is unreachable (AC-8)") |
| AC-9 | `calc-client.test.ts:155` ("one retry on a refused connection (AC-9)"), `:247` ("a refusal already seen beats the deadline (AC-9)") |
| AC-10 | `calc-client.test.ts:188` ("the deadline (AC-10)") |
| AC-11 | `app.test.ts:37` ("POST /api/v1/calculate — success (AC-11)", ×7) |
| AC-12 | behavioural `app.test.ts:78`; structural `no-arithmetic.test.ts:33` (3 tests) |
| AC-13 | `app.test.ts:186` ("malformed requests (AC-13)"), `:240` ("a body that is not JSON (AC-13)") |
| AC-14 | `app.test.ts:140` ("a downstream domain failure (AC-14)") |
| AC-15 | `app.test.ts:108` ("calc-service unavailable (AC-15)"), `:281`; live `server.test.ts:50` (502 with real client + real `fetch`) |
| AC-16 | every body assertion is `toStrictEqual`; `app.test.ts:281` ("one envelope, no internal detail (AC-15, AC-16)") |
| AC-17 | `app.test.ts:344` ("CORS (AC-17)", ×5) |
| AC-18 | `server.test.ts:50` ("start (AC-18)", ×5), `:105` ("node src/server.ts — the entry point (AC-18)"); default 3000 via `config.test.ts` |
| AC-19 | `apps/api-gateway/README.md` — docs criterion, checked line by line above |
| AC-20 | the gate (15/15 under Node 22 and Node 24); `GATEWAY_PORT` ≠ `CALC_SERVICE_PORT` (`config.test.ts` "ignores every other variable, calc-service's port included"); both services ran side by side in the smoke on distinct ports with no env collision. `npm run build` / live `npm run dev`: not in `Commands:`, not run (see AC-4) |
| AC-21 | `app.test.ts:309` ("recovery after an outage (AC-21)"); reproduced live in the smoke |

## Review trail

- `review.md` — **APPROVED**, zero findings across four lenses; no open blocker, major or minor. `review-verdict.md` = `APPROVED`.
- `review-spec.md` — CHANGES_REQUESTED (3 major, 6 minor), all nine marked `resolved` with a `fix:` line each; the fixes are spec/trail edits that are visible in `spec.md` / `acceptance-criteria.md` (AC-21, the split 502/504 row, the `CALC_SERVICE_TIMEOUT_MS` decision row, the `STATUS_BY_CODE` representation).
- `review-slice-1.md`, `review-slice-2.md` — APPROVED, no finding. `review-slice-3.md` — CHANGES_REQUESTED, one major (docs parity, `AGENTS.md:7`) marked `resolved` at its tail. Its resolution is inspection of a prose sentence — the only possible check for a docs finding, no command was blocked — and I re-verified it: `AGENTS.md:7` ends "the SPA does not yet call the gateway and today renders only the design-system showcase". `review-slice-verdict-3.md` still reads `CHANGES_REQUESTED` — a stale bookkeeping file, not an open finding (`review.md` says the same).
- No resolution rests on a blocked command: the only "not run" statements (`tdd-3.md:25`, `review-slice-3.md:37`) concern `npm run build` / live `npm run dev`, which were never in any step's `Commands:` and are scope notes, not findings.
- All review files and `shrink-comments.md` are non-empty durable records (8–14 KB; `shrink-comments.md` 1160 B, one line per rewritten file plus a "nothing to cut" list).
- Human-accepted minors recorded in `spec.md`: none needed — no finding was left open. Recorded deviation (not a finding): `subtask-4.md` says `app.ts` exports a ready-made `app`; it exports `createApp` only, because a ready-made app would read `process.env` at import time against `spec.md` § Approach — accepted in `review-slice-3.md` and `review.md`.
- Human decision on record: `CALC_SERVICE_TIMEOUT_MS` (P1's BE-13 pulled forward) — `tmp/user-story.md` § Context, `spec.md` § Resolved decisions.

## Merge note (for the human PR step — not a DoD failure)

`main` has advanced past this branch's fork point `fa452c0`: it now carries the merge of `task/p3-calculator-ui` (`4f6d94c`). Diffs above therefore use merge-base semantics (`main...HEAD`, 53 files, +2273/−47); a two-dot diff against `main`'s tip would show p3's frontend work as deletions and is not this task's change. Two files are modified on both sides and will conflict textually on merge:

- `AGENTS.md` — the "Tests: Vitest" line: this branch adds `apps/api-gateway/src/*.test.ts` and keeps "…`apps/sezzle-calculator` and `packages/ui` still have none"; `main`'s `apps/sezzle-calculator/src/scope-docs.test.ts` asserts that same line does **not** match `/still have none/` and names the frontend and `@repo/ui` suites. The resolved line must satisfy both: list the gateway suite *and* p3's suites, with the "still have none" clause gone.
- `package-lock.json` — additive on both sides; re-run `npm install` under npm 10 after the merge and check the lockfile diff stays additive (the branch's lockfile was written under npm 10.9.8 — `tdd-2.md` cycle 1 — because npm 12 rewrites unrelated entries).

The combined state (this gateway + p3's calculator UI) was not gated here; run the same gate once after the merge.

## Environment

Gate and smoke run from `/Users/hernanlaura/projects/personal/sezzle-calculator/.worktrees/p2-api-gateway`. Shell default is Node 24.20.0 / npm 12.0.2 (prints the expected `EBADDEVENGINES` warning); the pinned line Node 22.23.2 / npm 10.9.8 was used via `~/.nvm/versions/node/v22.23.2/bin` for the summarised gate and the smoke. `--force` and `--summarize` were added to the given command only to defeat the Turbo cache replay and capture per-task evidence; no other command was substituted. Smoke scripts live in the session scratchpad, not the repo; all spawned servers were killed and their ports confirmed released.
