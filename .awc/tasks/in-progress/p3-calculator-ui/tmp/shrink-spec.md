# shrink-spec — p3-calculator-ui

Wording only. Every criterion, scenario, subtask and slice boundary kept its
meaning; headings, order and counts are unchanged. The cuts were inside
paragraphs, list items and table cells, so line counts hold.

- `spec.md`: 118 -> 118 lines — cut restatement in the summary, approach and
  alternatives; folded the state-machine result rule into one line; dropped
  "which is exactly the seam", "invents no behaviour" style asides and the
  repeated Phase 4 rationale from the resolved-decisions table.
- `acceptance-criteria.md`: 37 -> 37 lines — AC-1 only: "keypad grid
  component ... into a grid" and "the package's own gallery" tightened,
  "no raw hex value" -> "no raw hex", final sentence shortened.
- `tmp/subtask-1.md`: 14 -> 14 lines — dropped "arrangement is its whole job"
  and "enumerates that barrel"/"in the same slice, so" filler.
- `tmp/subtask-2.md`: 18 -> 18 lines — merged the `App.tsx` sentences; cut
  "This is a surface change, so"; shortened the docs paragraph's lead-in.
- `tmp/subtask-3.md`: 19 -> 19 lines — cut "and no criterion has to describe
  feedback that does not exist" and "state that is unchanged" -> "state
  unchanged".
- `tmp/subtask-4.md`: 12 -> 12 lines — nothing to cut; read and left as is.
- `tmp/subtask-5.md`: 20 -> 20 lines — nothing to cut; read and left as is.
- `tmp/subtask-6.md`: 22 -> 22 lines — nothing to cut; read and left as is.
- `tmp/subtask-7.md`: 16 -> 16 lines — cut "which is the accepted shape of
  this phase" and "rather than a slogan".
- `tmp/subtasks.md`: 15 -> 15 lines — nothing to cut; read and left as is.

Total: 291 -> 291 lines across 10 files; prose tightened in 6 of them, the
other 4 already at their shortest.
